---
// utils imports
import { formatDate } from "../js/utils";

const { frontmatter, url } = Astro.props;

const { title, description, date, category, author, image } = frontmatter;
---

<div class="featured-post">
  <div class="featured-title-data-media">
    <h3>
      <a href={url}>{title}</a>
    </h3>
    <span>
      <span class="featured-post-date">{formatDate(date)}</span> • <a href={`/author/${author.slug}/`}>{author.name}</a> • <span class="article-category"><a href={`/category/${category.slug}/`}>{category.name}</a></span>
    </span>
  </div>
  <a class="featured__img-link" href={url}>
    <img class="featured-post-image" src={image.src} alt={image.alt}>
  </a>
  <div>
    <h3 class="featured-title">
      <a href={url}>{title}</a>
    </h3>
    <span class="featured-data">
      <span class="featured-post-date">{formatDate(date)}</span> • <a href={`/author/${author.slug}/`}>{author.name}</a> • <span class="article-category"><a href={`/category/${category.slug}/`}>{category.name}</a></span>
    </span>
    <p class="featured__description">{description}</p>
  </div>
</div>

<style>
  .featured-title-data-media {
    display: none;
    margin-bottom: 1.1rem;
  }
  .featured-post a {
    color: var(--text-clr-dark);
    text-decoration: none;
  }
  .featured-post h3 {
    font-size: 2rem;
    font-weight: 600;
    margin: 1.5rem 0 0.4rem;
  }
  .featured__img-link {
    display: block;
    width: 100%;
    height: 600px;
  }
  .featured__img-link img {
    width: 100%;
    height: 100%;
    border-radius: 6px;
    object-fit: cover;
  }
  .featured-post span {
    color: var(--text-clr-dark-m);
    /* opacity: 0.9; */
  }
  .featured-post-date {
    font-size: 0.95rem;
  }
  .featured-data a {
    color: var(--text-clr-dark-m);
  }
  .featured__description {
    font-size: 1.2rem;
    font-weight: 400;
    margin-top: 1rem;
  }

  @media screen and (max-width: 1024px) {
    .featured__img-link {
      height: 550px;
    }
  }
  @media screen and (max-width: 768px) {
    .featured-post h3 {
      font-size: 1.75rem;
    }
    .featured__img-link {
      height: 500px;
    }
    .featured-post span {
      font-size: 0.95rem;
    }
    .featured-post-date {
      font-size: 0.85rem !important;
    }
    .featured__description {
      font-size: 1.1rem;
      margin-top: 1rem;
    }
  }
  @media screen and (max-width: 720px) {
    .featured-title,
    .featured-data {
      display: none;
    }
    .featured-title-data-media {
      display: block;
    }
    .featured-post h3 {
      margin: 0 0 0.4rem;
    }
  }
  @media screen and (max-width: 640px) {
    .featured__description {
      font-size: 1rem;
    }
  }
  
</style>