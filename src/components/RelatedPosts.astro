---
// utils imports
import { slugify } from "../js/utils";

const { relatedPosts } = Astro.props;
---

{
  relatedPosts.map((post) => (
    <div class="related-post">
      <a href={post.url}>
        <img class="related-post-image" src={post.frontmatter.image.src} alt={post.frontmatter.image.alt}>
      </a>
      <h3>
        <a href={post.url}>{post.frontmatter.title}</a>
      </h3>
      <span>
        <a href={`/author/${post.frontmatter.author.slug}/`}>{post.frontmatter.author.name}</a> • <a class="article-category" href={`/category/${slugify(post.frontmatter.category.slug)}/`}>{post.frontmatter.category.name}</a> 
      </span>
    </div>   
  ))
}

<style>
  .related-post:not(:first-child)  {
    border-bottom: 1px solid rgb(153, 152, 152, 0.5);
    padding: 0 0.5rem 1rem;
  }
  .related-post:not(:first-child)  {
    padding-top: 1rem;
  }
  .related-post-image {
    height: 280px;
    width: 100%;
    object-fit: cover;
  }
  .related-post a {
    color: var(--primary-clr-dark);
    text-decoration: none;
  }
  .related-post h3 {
    font-size: 1.2rem;
    font-weight: 600;
    margin-top: 0.8rem;
  }
</style>