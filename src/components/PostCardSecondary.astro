---
// utils imports
import { formatDate } from "../js/utils";

const { frontmatter, url } = Astro.props;

const { title, description, date, category, author, image } = frontmatter;
---

<div class="secondary-post">
  <a class="secondary__img-link" href={url}>
    <img class="secondary-post-image" src={image.src} alt={image.alt}>
  </a>
  <div>
    <h3>
      <a href={url}>{title}</a>
    </h3>
    <span>
      <span class="secondary-post-date">{formatDate(date)}</span> • <a href={`/author/${author.slug}/`}>{author.name}</a>
    </span>
<!--     
    <p class="secondary__description">{description}</p> -->
  </div>
</div>

<style>
  .secondary-post a {
    color: var(--primary-clr-dark);
    text-decoration: none;
  }
  .secondary-post h3 {
    font-size: 1.35rem;
    font-weight: 600;
    padding: 0.5rem 0 0.4rem;
  }
  .secondary-post-date {
    font-size: 0.9rem;
  }
  .secondary__img-link {
    display: block;
    width: 100%;
    height: 230px;
  }
  .secondary__img-link img {
    width: 100%;
    height: 100%;
    border-radius: 6px;
    object-fit: cover;
  }
  .secondary__description {
    font-size: 1.1rem;
    font-weight: 300;
  }
  .secondary-post span {
    opacity: 0.9;
  }

  @media screen and (max-width: 768px) {
    .secondary-post h3 {
      font-size: 1.2rem;
    }
    .secondary__img-link {
      height: 210px;
    }
    .secondary-post span {
      font-size: 0.95rem;
    }
    .secondary-post-date {
      font-size: 0.85rem !important;
    }
  }

  @media screen and (max-width: 720px) {
    .secondary-post {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 0.5rem 0 !important;
    }
    .secondary-post:not(:last-child) {
      border-bottom: 1px solid rgb(153, 152, 152, 0.5);
    }
    .secondary__img-link {
      width: 160px;
      height: 160px;
      flex-shrink: 0;
    }
    .secondary-post-image {
      height: 100%;
      width: 100%;
    }
    .secondary-post h3 {
      font-size: 1.25rem;
    }
    .secondary-post span {
      font-size: 0.95rem;
    }
    .secondary-post-date {
      font-size: 0.9rem !important;
    }
  }

  @media screen and (max-width: 640px) {
    .secondary__img-link {
      width: 140px;
      height: 140px;
    }
  }

  @media screen and (max-width: 480px) {
    .secondary__img-link {
      width: 120px;
      height: 120px;
    }
    .secondary-post h3 {
      font-size: 1.2rem;
    }
    .secondary-post span {
      font-size: 0.9rem;
    }
    .secondary-post-date {
      font-size: 0.85rem !important;
    }
  }
</style>